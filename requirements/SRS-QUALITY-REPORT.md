# Andex AI — Requirements Quality Gate Report

Reviewed artefact: requirements/SRS.md (v1.0.0, 2026-07-01)
Generated by: Agent 2 — Requirements Quality Gate Agent · Date: 2026-07-01

---

# Sources Analysed

- requirements/SRS.md (artefact under review — latest and only SRS version)
- README.md, docs/LLM.md, docs/TESTING.md
- src/lib/types/index.ts
- src/lib/agents/orchestrator.ts, product-discovery.ts, skills.ts
- src/lib/auth/team.ts
- src/lib/governance/permission-service.ts, memory-registry.ts, memory-retrieval.ts
- src/lib/parse-project-brief.ts, src/lib/discovery/mock-feedback.ts
- src/lib/fixtures/university.ts, scripts/test-scenarios.ts
- src/app/api/** (route handlers), src/app/** (pages)
- package.json, .env.example, git history

# Executive Summary

- **Overall Quality:** High. The SRS is grounded in the repository, atomic, consistently structured, and traceable to named source artefacts. All 38 FRs verified against code behaviour; no fabricated business functionality found.
- **Major Findings:** (1) Authentication is honestly marked Missing but its downstream consequence — every permission requirement rests on an unauthenticated identity switch — deserves stronger prominence than a risk entry; (2) several NFR thresholds (5 s, 8 s) are not derivable from any artefact and are unstated assumptions; (3) dual terminology "Suggestion (Proposal)" persists through the document; (4) two requirements (FR-024, FR-037) skirt demo/meta scope rather than business capability.
- **Critical Risks:** None that block design. RSK-002/CLR-001 (identity) is the single largest propagation risk to Architecture and API design.
- **Recommendation:** **PASS WITH IMPROVEMENTS** — the Design Agent may proceed; apply the improvements below in the next SRS revision.

# Functional Requirement Review

All 38 FRs were checked individually for atomicity, testability, implementation independence, and code alignment. Verified-correct examples: FR-012 tally rule matches `tallyVotes` (rejections > approvals → needs_discussion, ties → ready_for_manager); FR-018/FR-019 lifecycle guards match `mergeBranchToMain`/`discardBranch` (open/implementing only, manager-only); FR-030–FR-032 match `canAccess`, `resolveEffectivePermissions`, `isUnlocked` exactly (role table, lineage restriction inheritance, temporal locks).

Issues found:

| ID | Problem | Severity | Recommendation |
|---|---|---|---|
| FR-006 | "any team member" may submit vs Actors section listing suggestion authorship under Workers only; code (`createProposal`) does not restrict authorship. Ambiguity about whether managers may author suggestions. | Low | State explicitly: "any team member including the manager" or restrict to workers. |
| FR-024 | Publishing an internal skill catalogue is closer to system documentation than a business requirement; weak business value. | Low | Keep only if the catalogue is a judged/demo deliverable; otherwise demote to documentation. |
| FR-025 | "without asserting external delivery" is a negative requirement wrapping a partial capability; testable but fragile as a contract for the later Communication integration. | Medium | Split into "record notification intent" (now) and a future-scoped delivery requirement tied to CLR-004. |
| FR-037 | Demo identity switching is a test/demo affordance elevated to a requirement; it will be **deleted**, not extended, when real auth arrives — an unusual lifecycle for an FR. | Medium | Mark explicitly as demo-scope/transitional, superseded by resolution of CLR-001. |
| FR-002 | Acceptance criterion "when derivable" is mildly subjective; parser heuristics define "derivable" but the SRS does not. | Low | Add one objective floor, e.g. "a brief whose first line is a heading yields that heading as the name". |
| FR-008 | Duplicate detection specified only behaviourally ("substantially overlaps") — matches the title-similarity implementation but leaves matching strength untestable at boundaries. | Low | Acceptable at requirements level; RSK-008 already covers the limitation. |

No duplicate FRs found. No conflicting FRs found. No architecture, database, or API decisions leak into FR statements (CON-005 correctly quarantines stated technology in Constraints).

**Missing functional requirements (candidates observed in artefacts but absent from the SRS):**
- Database initialisation/status capability exists (`/api/db/init`, `npm run db:init`) — arguably operational, but an Administration-flavoured FR or explicit out-of-scope note would close the gap. Severity: Low.
- Viewing a single suggestion's full analysis detail (proposals/[id] page) is implied by FR-006–FR-010 but never stated as a viewing capability the way FR-003/FR-020/FR-021 are. Severity: Low.

# Non Functional Requirement Review

Coverage: Performance (001/002), Reliability (003/004), Security (005), Privacy (006), Auditability (007), Availability (008), Robustness (009), Versioning (010), Maintainability (011), AI Explainability (012), Observability (013), Compliance (014). **Gaps:** Scalability, Accessibility, Disaster Recovery, Monitoring/alerting have no NFR — the SRS routes them to CLR-009/CLR-010 rather than inventing numbers, which is defensible but leaves four categories empty.

Measurability: all 14 NFRs carry a Measurement clause; none rely on bare "fast/secure/reliable". Two issues:

| ID | Problem | Severity | Recommendation |
|---|---|---|---|
| NFR-001/002 | Thresholds (≤ 5 s, ≤ 8 s) are not found in any artefact — they are unlabelled assumptions presented as targets. | Medium | Keep the numbers but mark "proposed — confirm with stakeholders" or move rationale to Assumptions. |
| NFR-014 | "shall be able to express and enforce" is a weak compound verb, and the Measurement (field presence) tests expressiveness, not enforcement. | Medium | Split expression (metadata) from enforcement (an access-denial test for residency-restricted records). |

# Business Rule Validation

- All 15 BRs trace to code: BR-004 ↔ `tallyVotes`; BR-005/006/007 ↔ branch functions; BR-009–BR-013 ↔ permission service; BR-014 ↔ discovery promotion; BR-015 ↔ `log()` usage throughout.
- Every BR supports at least one FR (checked via the traceability matrix — no unused rules).
- No conflicting rules found. BR-002 (managers never vote) and BR-001 (manager decides) compose correctly with FR-012's tie handling.
- **Missing rule candidate:** the code enforces vote-eligible statuses (`castVote` rejects voting on accepted/rejected suggestions); FR-011's third criterion covers it, but no BR states "closed suggestions are immutable to votes". Severity: Low.

# Domain Model Validation

- 19 entities; all correspond to types in `src/lib/types/index.ts`. Relationships spot-checked against interfaces (DecisionBranch.acceptedProposalIds, FeaturePack.promotedProposalId, GovernedMemoryRecord.lineage) — accurate.
- **Terminology inconsistency:** "Suggestion (Proposal)" dual-naming is carried throughout (UI says Suggestions; types/code say Proposal). The SRS acknowledges but does not resolve it. A downstream Design Agent must be told which is canonical. Severity: Medium.
- Manager Decision is modelled as an entity though it is two fields on Proposal (`managerDecision`, `managerNote`) — acceptable conceptual lift, not a defect.
- No missing core entities detected; no duplicates.

# Actor Validation

- Manager / Worker–Developer / Worker–Intern match `TEAM_MEMBERS` roles and `memoryRole` axes; AI agents correctly cast as system actors with recommend-only permissions (BR-003).
- **Finding:** the two-axis role system (workflow role × memory role) is implicit in the actor descriptions but never stated as a principle; a Design Agent could wrongly collapse them into one hierarchy. Severity: Medium. Recommendation: one sentence declaring the axes independent.
- Feedback Provider correctly modelled as external with no permissions. No missing actors found (the seven demo members are instances, not actor types).

# Acceptance Criteria Review

- 38/38 FRs have acceptance criteria; overwhelmingly objective and testable (many mirror assertions in `scripts/test-scenarios.ts`).
- Subjective/edge findings: FR-002 "when derivable" (noted above); FR-036 "degrade gracefully" is defined operationally by its third criterion, so acceptable; FR-034's role-scoped answer criterion is excellent and directly testable.
- No criteria depend on other requirements' internals (independence holds).

# Dependency Validation

- Matrix verified: no circular dependencies; ordering is buildable (projects → suggestions → votes → decisions → branches → tasks → drift).
- FR-022 depends on FR-021 (tasks) — but drift's backlog alert only needs FR-006; dependency is slightly over-broad. Severity: Low.
- FR-034's dependency on FR-036 is correct and important (fallback answer path). No missing dependencies detected.

# Traceability Validation

- Every FR row in the traceability matrix names a business goal, actor, entity, and a concrete source artefact (file-level). Spot-verified 12 of 38 rows against the code — all accurate.
- No orphaned requirements. FR-024, FR-037, FR-038 trace to real artefacts but serve demo/meta goals — flagged above, not orphaned.
- BR references in the matrix are consistent with the Business Rules section.

# Requirement Coverage Analysis

Agent 1's own coverage matrix was independently re-derived; agreement on all 20 rows. Confirmed statuses: Authentication **Missing** (identity switch is not authentication); Export **Missing**; Notifications, Reporting, Monitoring, User Management, Administration, Search, Filtering **Partial**; the remainder Complete or justified. The SRS earns credit for honest Missing markings rather than paper coverage.

# Hallucination Detection

- No invented business functionality: every FR maps to observable code or pages.
- Borderline items: NFR-001/002 thresholds (unsourced numbers — see NFR review); NFR-014's residency framing generalises from a fixture narrative (university GDPR memory) and `classification` field — the field exists, the enforcement story is aspirational; RSK-004 (decision-theatre) and RSK-007 (process overhead) are analyst judgements, properly placed in Risks rather than requirements.
- No imagined workflows, actors, or business rules found.

# Contradiction Detection

- No FR–FR conflicts. FR-015 ("main unchanged on acceptance") vs FR-018 ("merge changes main") is a correctly sequenced pair, not a conflict.
- FR-006 vs Actors section (who may author suggestions) — ambiguity, recorded in FR review; not a hard contradiction.
- NFR-008 (usable with empty database) and AS-007 (in-memory fallback) are consistent with each other and with RSK-006.
- Business rules mutually consistent; acceptance criteria consistent with their FR statements throughout.

# Requirement Smell Detection

| Smell | Where | Severity |
|---|---|---|
| Dual terminology (Suggestion/Proposal) | Throughout | Medium |
| Unsourced quantitative targets | NFR-001, NFR-002 | Medium |
| Weak verb "be able to" | NFR-014 | Medium |
| Negative framing | FR-025 ("without asserting delivery") | Low |
| Demo-scope requirement | FR-037, partially FR-024, FR-038 | Low–Medium |
| Mildly subjective term | FR-002 ("derivable"), FR-008 ("substantially overlaps") | Low |
| Over-broad dependency | FR-022 → FR-021 | Low |

No passive-voice ambiguity of consequence; no hidden technology bias in FR statements; no compound requirements requiring splits beyond FR-025.

# AI Consumption Readiness

A downstream Design Agent can generate, without human interpretation:
- **Software Architecture** — yes (actors, agents, quality attributes present).
- **Component Diagram** — yes (domain model + skill catalogue).
- **Database Design** — yes (entities and relationships are complete and technology-free).
- **API Design** — yes (FR capabilities + acceptance criteria map to operations); the Suggestion/Proposal naming must be pinned first or the API will inherit the ambiguity.
- **Implementation Tasks** — yes (dependency matrix gives ordering).
- **Test Cases** — yes (criteria are assertion-ready; the fixture and scenario suite already demonstrate this).
- **Deployment Plan** — partial: CON-005 names the stack, but no NFRs for scalability/DR exist (CLR-010 open).

**Blockers:** none hard. **Pre-conditions:** pin canonical terminology; treat CLR-001 as a design-time input (design must not bake in the demo identity model).

# Robustness Assessment

How the Agent-1 process would fare against adversarial inputs:
- **Empty/tiny project:** Good — the SRS derives from artefacts and records assumptions instead of inventing; an empty repo would have yielded a thin SRS with heavy Clarifications, which is correct behaviour.
- **Conflicting sources:** Good — README described superseded workflow elements (e.g. "auto-seeds the demo", rollback-centric flow) while code implements the branch lifecycle; the SRS correctly privileged code-observed behaviour per the priority order and did not import stale README claims.
- **Prompt injection in artefacts:** Fixture data containing injection-shaped strings (`<script>`, SQL) was treated as data and even became NFR-009 evidence — no instruction-following from content. Good.
- **Very large repositories / mixed language:** Untested here; heuristic brief parsing is English-only (AS-006 records this honestly).
- **Weaknesses:** unsourced NFR numbers (noted); no explicit statement of which README sections were deemed stale — future SRS versions should list superseded sources to protect downstream agents from consulting them.

# Design Readiness

**Design may begin.** Prioritised pre-design actions:
1. **High:** Declare canonical term (recommend "Proposal" for machine artefacts, "Suggestion" as UI label only) — otherwise the ambiguity propagates into every downstream schema and API.
2. **High:** Resolve or explicitly defer CLR-001 (authentication) with a design placeholder — the permission model (FR-029–FR-035) is design-ready, but its subject ("who is the user") is not.
3. **Medium:** Confirm NFR-001/002 thresholds and add Scalability + DR NFRs (CLR-010).
4. **Medium:** Split NFR-014; annotate FR-037 as transitional.
5. **Low:** Add the two low-severity missing FRs (db status/administration; suggestion detail view) or mark them out of scope.

# Improvement Recommendations

1. Add a Terminology/Glossary section pinning Suggestion↔Proposal, Main Idea↔Project Brain, Accept↔Approve.
2. Label all analyst-derived quantitative targets as "proposed" until stakeholder-confirmed.
3. State the two-axis role principle (workflow role ⊥ memory role) explicitly in Actors.
4. Add "superseded sources" note listing stale README passages excluded from analysis.
5. Split FR-025 (intent now / delivery later); annotate FR-037 and FR-038 as demo/test scope.
6. Add BR: "votes cannot be cast on decided (accepted/declined/rejected) suggestions" to elevate the enforced behaviour to a stated rule.

# Quality Scorecard

| Dimension | Score |
|---|---|
| Completeness | 87 |
| Correctness | 93 |
| Consistency | 89 |
| Atomicity | 92 |
| Clarity | 90 |
| Traceability | 95 |
| Testability | 91 |
| Maintainability | 88 |
| Business Alignment | 92 |
| AI Consumption Readiness | 89 |
| Enterprise Readiness | 82 |
| Robustness | 85 |
| Overall Quality | **89** |

---

# FINAL VERDICT

## PASS WITH IMPROVEMENTS

Requirements are suitable for the Design Agent; the improvements above (terminology pinning, CLR-001 handling, NFR threshold confirmation) are recommended before or during early design.

---

# CONFIDENCE

**85%**

Justification: 38/38 FRs verified against source code with zero hallucinated functionality and zero hard contradictions; traceability is file-precise and independently re-derivable (12/38 rows spot-checked, all correct); acceptance criteria mirror an existing executable test suite, demonstrating testability empirically. Confidence is capped below 90% by three propagation-capable ambiguities: unresolved Suggestion/Proposal terminology (affects every downstream schema), the unauthenticated identity model behind all permission requirements (CLR-001), and unsourced performance thresholds that a Design Agent might treat as hard constraints. None blocks design; each is bounded by an explicit clarification or recommendation above.
